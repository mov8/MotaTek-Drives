Redmi jumbleboy
deleting temp files C:\Users\james\AppData\Local\Temp

Problems with version updates on devices:
Experienced a problem for a little while where after a build
have to do a restart before the latest build is put on the 
device. Then when running without PC reverts back to previous 
version. The problem started when swapping between debug and
release versions. 
Cure follow 4 steps:
1 flutter clean - removes build and the dependencies
2 flutter pub get - reinstalls dependencies
3 remove the version from the device 
4 rebuild on the device

Flutter Intellisense very SLOW
1   Open Command Pallet Ctrl+Shift+P
2   Restart Analysis Server + Enter
3   Once spinner in BRHS stops spinning should be a lot faster
Look at the end of the Gemini thread "Porting Flutter App to the Web" for other speed issue possibilities & solutions

If Flutter doesn't compile try:
flutter run -v 

Problems with installing Hive
To create an Adapter to store a non native type look trip_model.dart 
dart run build_runner build creates the boilerplate file trip_model.g.dart that Hive uses
build_runner looks at all the dart files in the project and is very fussy, all the issues
must be resolved before it will work things like setState(() => ()); upset it and have to
be changes to setState(() {});  which in fairness the docs do state,

1 flutter clean
2 flutter pub get 
3 dart run build_runner clean 
4 dart run build_runner build --delete-conflicting-outputs 








Warning: In index.html:45: Flutter's service worker is deprecated and will be removed in a future Flutter release. See https://github.com/flutter/flutter/issues/156910 for more details.
Warning: In index.html:59: "FlutterLoader.loadEntrypoint" is deprecated. Use "FlutterLoader.load" instead. See https://docs.flutter.dev/platform-integration/web/initialization for more details.

flutter upgrade

Error with wrongParent
The ParentDataWidget Expanded(flex: 1) wants to apply ParentData of type FlexParentData to a RenderObject
This is caused by an Expanded being used for a parent widget other than a Column or Row that it intended for. 
It works in debug mode, but causes a blank screen in release mode.

Lesson from this any problems in release mode may well be 
due to problems shown but ignored in debug mode. Sorting all
debug problems is never a bad idea!

Error of unconstrained size:
Failed assertion: line 2251 pos 12: 'hasSize'
This is caused where an expandable widget is the child and grandchild of an expandible widget - Flutter has 
no idea how to lay it out. Must have a constrained container in the tree. 

API tips
ssh root@64.227.34.217
docker exec -it root-db-1 /bin/bash   <-- get inside drives-db-1 container
mariadb -uroot -pmysecret  <-- Start mariadb

To copy a new app version to the Droplet
scp /home/james/Python-3.10.0/Drives/drives/apks/motatek_drives.apk root@64.227.34.217:/root/app/static/apks/motatek_drives.apk

UPDATING BACKEND:
-----------------
DON'T FORGET TO CHANGE CONFIG.PY IF GOING FROM LOCAL TO REMOTE HOST
===================================================================
1 If container running on local machine docker down <container name> docker container ls
2 Remove the image docker rmi <image id> from docker image ls
3 Make the changes to the backend and make sure file is saved
4 Build image - docker build -t motatekjames/drives:latest ./api <-- in Python-3.10.0/Drives
5 $ docker login -u motatekjames -p nicetrip2drive <-- Login to Dockerhub
6 $ docker push motatekjames/drives:latest 
7 ssh into Droplet - ssh root@64.227.34.217  NOT SUDO
8 In droplet docker-compose down  <-- In root folder
9 Remove the image to be replaced
10 docker pull motatekjames/drives:latest
11 docker-compose up -d  to start the backend running.
12 Don't forget any changes to nginx.conf must be followed by building the nginx container.


docker exec -it root-db-1 /bin/bash     <-- get inside drives-db-1 container
mariadb -uroot -pmysecret               <-- start mariadb in db container

Git stash
git stash <-- reverts back to last commit
git stash list <-- shows all the stashes
git stash apply <-- reloads the stashed data - git assumes it'll be the last stash, but you can specify stash ie git stash apply stash@{2}



Todo-Tree: Failed to find vscode-ripgrep - please install ripgrep manually and set 'todo-tree.ripgrep' to point to the executable